@AGENTS.md

## Design tokens

- **Primary color:** `#664fa1` (IPN purple) — available as `bg-ipn`, `text-ipn`, `border-ipn` via the `@theme` block in `globals.css`
- **Logos:** `/assets/` folder — currently `.webp`, will be updated to `.png` soon; update `<Image>` `src` paths when that happens
- **Font:** Geist Sans (body) + Geist Mono (code) — loaded in `layout.tsx`
